# Test basic heredoc syntax and cmp command

exec echo hello
cmp stdout <<EOF
hello
EOF

# Test heredoc with multiple lines
exec echo -e "line1\nline2\nline3"
cmp stdout <<MARKER
line1
line2
line3
MARKER

# Test empty heredoc
exec echo ""
cmp stdout <<EMPTY

EMPTY
